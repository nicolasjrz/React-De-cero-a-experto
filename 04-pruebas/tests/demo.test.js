describe("prueba 1", () => {
  test("esta prueba no debe de fallas", () => {
    // 1 INICIALIZACION
    // 2 ESTIMULO
    // 3 OBSERVAR EL COMPORTRAMIENDO

    const msj = "hola mundo";

    const msj2 = msj.trim();

    expect(msj).toBe(msj2);
  });
});
